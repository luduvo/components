import * as React from "react"
// we opt to use our own page on luduvo.com, rather than using this shadcn registry template.
// we just build from here and have a script that copies the /r/ output to our main site

export default function Home() {
  return (
    <></>
  )
}
